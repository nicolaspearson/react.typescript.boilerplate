import * as React from 'react';

import VendorSlider from 'rc-slider';

import './style.scss';

const Slider = (props: any) => (
	<div>
		<VendorSlider {...props} />
	</div>
);

export default Slider;
